{
  "name": "radar-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Testes das regras do Firestore. Vive AQUI, e não na raiz, porque o app servido nao tem package.json e nao pode ganhar um: a Vercel publica a raiz. Dependencia so de desenvolvimento.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project radar-test \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1"
  }
}
